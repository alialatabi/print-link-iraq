/**
 * Storage helpers — canonical implementations have moved to `@/lib/designUtils`.
 * Re-exporting here preserves all existing import paths without changes.
 */
export { getDesignSignedUrl } from '@/lib/designUtils';
